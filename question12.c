#include<stdio.h>
#include<conio.h>
#include<math.h>
void main()
{
	int number,result;
	printf("Enter number:");
	scanf("%d",&number);
	result= sqrt(number);
	printf("The square number of %d is %d.",number,result);
	return 0;
	
}
